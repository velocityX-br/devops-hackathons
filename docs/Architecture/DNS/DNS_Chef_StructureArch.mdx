---
sidebar_position: 2
title: DNS Chef Structure
description: Interactive draw.io diagram — DNS Chef structure architecture
---

import Drawio from '@theme/Drawio'
import chefArch from '!!raw-loader!./DNS_Chef_StructureArch.drawio'

# DNS Chef Structure

下方图表由 `.drawio` 源文件直接渲染，可缩放、切换图层，并在 lightbox 中全屏查看。

<Drawio
  content={chefArch}
  toolbar="zoom layers lightbox"
  maxHeight={900}
/>
