
> :warning:  **This script is obsolete and will not give correct results with pppconf 5 and ppptune 3!** 
> **The pppconf part is available in the pppconf package (>=5.0.2) as `pppconf_check` and the ppptune part in the ppptune package (>=3.0.0) as `ppptune_check`.**


- ServeMux 是 Go 标准库中内置的HTTP 请求多路复用器，也就是 URL 路由管理器。


