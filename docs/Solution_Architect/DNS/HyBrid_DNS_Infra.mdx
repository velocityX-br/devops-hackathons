---
sidebar_position: 5
slug: Hybrid_DNS_Infra
title: Hybrid DNS Architecture
description: Interactive draw.io diagram — hybrid DNS infrastructure spanning classic HA and Kubernetes-based services
---

import Drawio from '@theme/Drawio'
import hybridDns from '!!raw-loader!./HyBrid_DNS_Infra.drawio'

# Hybrid DNS Architecture

End-to-end hybrid DNS infrastructure — classic highly available hidden-primary paths alongside Kubernetes-based DNS services.
The diagram below is rendered from the `.drawio` source — zoom, toggle layers, and open fullscreen via lightbox.

<Drawio
  content={hybridDns}
  toolbar="zoom layers lightbox"
  maxHeight={900}
/>
