---
sidebar_position: 3
title: Multi-AZ DNS Build Flow
description: Interactive draw.io diagram — Multi-AZ DNS build flow
---

import Drawio from '@theme/Drawio'
import multiAzFlow from '!!raw-loader!./MultiAZ_DNS_BuildFlow.drawio'

# Multi-AZ DNS Build Flow

下方图表由 `.drawio` 源文件直接渲染，可缩放、切换图层，并在 lightbox 中全屏查看。

<Drawio
  content={multiAzFlow}
  toolbar="zoom layers lightbox"
  maxHeight={900}
/>
